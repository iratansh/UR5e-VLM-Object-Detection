# Quick Start Guide for Simulation

## 1. Install Dependencies

```bash
# Install Gazebo packages
sudo apt update
sudo apt install -y \
  ros-humble-gazebo-ros-pkgs \
  ros-humble-ros2-control \
  ros-humble-ros2-controllers \
  ros-humble-gazebo-ros2-control \
  ros-humble-controller-manager \
  ros-humble-joint-trajectory-controller \
  ros-humble-joint-state-broadcaster

# Install UR and MoveIt packages
sudo apt install -y \
  ros-humble-ur \
  ros-humble-ur-description \
  ros-humble-ur-moveit-config \
  ros-humble-moveit \
  ros-humble-moveit-ros-planning \
  ros-humble-moveit-ros-visualization

# Python dependencies (updated for Whisper ASR, construction VLM, and TTS)
# Note: Whisper requires ffmpeg to be installed and available on PATH
# Ubuntu/Debian: sudo apt install -y ffmpeg
# macOS (Homebrew): brew install ffmpeg

pip3 install \
  opencv-python \
  pyrealsense2 \
  transformers \
  torch \
  sounddevice \
  openai-whisper \
  pyttsx3 \
  sentence-transformers

# Optional (research / performance):
# pip3 install accelerate safetensors
# Optional alternative TTS engine (future):
# pip3 install TTS  # Coqui TTS